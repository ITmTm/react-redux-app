{
	"version": 2,
	"builds": [
		{
			"src": "src/**/*",
			"use": "@vercel/static-build",
			"config": {
				"distDir": "build"
			}
		},
		{
			"src": "heroes.json",
			"use": "@vercel/static-build"
		}
	],
	"routes": [
		{
			"src": "/api/heroes",
			"dest": "/heroes.json"
		}
	]
}